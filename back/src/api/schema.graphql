type ExpenseData {
  userId: String!
  expenseDate: String!
  regFixedCost: Int!
  irregFixedCost: Int!
  regVarCost: Int!
  irregVarCost: Int!
}

type Query {
  getExpData(userId: String!): [ExpenseData]
}
